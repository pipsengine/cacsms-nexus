"use client";

import { Activity, AlertTriangle, Bot, CheckCircle2, Download, Menu, RefreshCw, Search, ShieldCheck, Signal, Workflow } from "lucide-react";
import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "@/components/ui/dropdown-menu";
import { cn } from "@/lib/utils";
import { useSymbolSync } from "../hooks/use-symbol-sync";
import type { SymbolSeverity, SymbolTone } from "../types/symbol-sync.types";

const variants: Record<SymbolTone, "success" | "warning" | "destructive" | "default" | "secondary"> = { Healthy: "success", Watch: "warning", Degraded: "warning", Critical: "destructive", Offline: "destructive", Syncing: "default", Inactive: "secondary" };
const borders: Record<SymbolTone, string> = { Healthy: "border-t-emerald-500", Watch: "border-t-amber-500", Degraded: "border-t-amber-500", Critical: "border-t-red-500", Offline: "border-t-red-500", Syncing: "border-t-blue-500", Inactive: "border-t-slate-400" };
const time = (value: string) => new Date(value).toLocaleTimeString([], { hour: "2-digit", minute: "2-digit", second: "2-digit" });
function Status({ value }: { value: SymbolTone }) { return <Badge variant={variants[value]}>{value}</Badge>; }
function Severity({ value }: { value: SymbolSeverity }) { return <Badge variant={value === "Critical" ? "destructive" : value === "Warning" ? "warning" : "secondary"}>{value}</Badge>; }
function Heading({ icon: Icon, title, detail }: { icon: typeof Activity; title: string; detail: string }) {
  return <CardHeader className="pb-4"><CardTitle className="flex items-center gap-2 text-lg"><Icon className="h-5 w-5 text-blue-600" />{title}</CardTitle><p className="mt-1 text-xs text-slate-500">{detail}</p></CardHeader>;
}
function Gauge({ label, value, dangerous = false }: { label: string; value: number; dangerous?: boolean }) {
  return <div><div className="mb-1 flex justify-between text-xs text-slate-600"><span>{label}</span><strong>{value}</strong></div><div className="h-2 rounded-full bg-slate-100"><div className={cn("h-2 rounded-full", dangerous ? "bg-red-500" : "bg-blue-600")} style={{ width: `${Math.min(100, value)}%` }} /></div></div>;
}

export function SymbolSyncDashboard() {
  const query = useSymbolSync();
  const [selectedId, setSelectedId] = useState("symbol-5");
  const [search, setSearch] = useState("");
  const [filter, setFilter] = useState("All");
  const [issueFilter, setIssueFilter] = useState("All");
  const [notice, setNotice] = useState<string | null>(null);
  if (query.isError) return <div className="mx-auto max-w-[1900px] px-4 py-6"><Card className="border-red-200 p-6"><h1 className="text-xl font-semibold">Symbol Sync unavailable</h1><p className="mt-2 text-sm text-slate-600">Symbol registry telemetry could not be loaded; no mapping operation was issued.</p><Button className="mt-4" onClick={() => query.refetch()}>Retry</Button></Card></div>;
  if (query.isLoading || !query.data) return <div className="mx-auto max-w-[1900px] px-4 py-6 text-sm text-slate-600">Loading symbol synchronization telemetry...</div>;
  const data = query.data;
  const selected = data.symbols.find((symbol) => symbol.id === selectedId) ?? data.symbols[0];
  const rows = data.symbols.filter((symbol) => `${symbol.brokerSymbol} ${symbol.normalizedSymbol} ${symbol.brokerName} ${symbol.assetClass}`.toLowerCase().includes(search.toLowerCase())).filter((symbol) => filter === "All" || symbol.mappingStatus === filter || symbol.feedStatus === filter || symbol.riskLevel === filter);
  const visibleIssues = data.issues.filter((issue) => issueFilter === "All" || issue.issueType === issueFilter || issue.severity === issueFilter);
  async function command(label: string, path: string, body?: Record<string, unknown>, method?: "POST" | "PATCH") {
    if (!window.confirm(`Confirm ${label.toLowerCase()}? This symbol operation will be audit-logged.`)) return;
    setNotice(null);
    try {
      await query.action.mutateAsync({ path, method, body: { confirmed: true, ...body } });
      setNotice(`${label} completed and was recorded in the audit trail.`);
    } catch (error) { setNotice(error instanceof Error ? error.message : "Symbol action failed."); }
  }
  function exportReport() {
    const blob = new Blob([JSON.stringify(data, null, 2)], { type: "application/json" });
    const anchor = document.createElement("a"); anchor.href = URL.createObjectURL(blob); anchor.download = "symbol-sync-report.json"; anchor.click(); URL.revokeObjectURL(anchor.href);
  }
  const actions = [
    { label: "Sync All Symbols", path: "/api/mt5/symbol-sync/sync", allowed: data.permissions.canSync },
    { label: "Validate Selected", path: `/api/mt5/symbol-sync/symbols/${selected.id}/validate`, allowed: data.permissions.canValidate },
    { label: "Run Diagnostics", path: "/api/mt5/symbol-sync/diagnostics", allowed: data.permissions.canDiagnostics }
  ];
  return <div className="mx-auto flex w-full max-w-[1900px] flex-col gap-4 px-4 py-5 sm:px-6 lg:px-8">
    <section className="sticky top-14 z-20 rounded-2xl border border-slate-200 bg-white/95 shadow-card backdrop-blur">
      <div className="h-1.5 rounded-t-2xl bg-gradient-to-r from-blue-600 via-emerald-500 to-purple-600" />
      <div className="flex flex-col gap-5 p-5 xl:flex-row xl:justify-between"><div>
        <p className="text-xs font-semibold uppercase tracking-wide text-slate-500">MT5 Infrastructure &amp; Broker Connectivity / Symbol Sync</p>
        <div className="mt-2 flex flex-wrap items-center gap-2"><h1 className="text-3xl font-semibold tracking-tight text-slate-950">Symbol Sync</h1><Badge variant={query.streamConnected ? "success" : "warning"}><Activity className="mr-1 h-3 w-3" />{query.streamConnected ? "Live stream" : "Reconnecting"}</Badge></div>
        <p className="mt-2 max-w-4xl text-sm text-slate-600">Real-time MT5 instrument normalization, broker symbol mapping, market-data freshness, spread validation, and execution readiness.</p>
        <p className="mt-3 text-xs text-slate-500">Mode: {data.meta.monitoringMode} | Role: {data.permissions.role} | Selected: {selected.brokerSymbol} | Updated: {time(data.meta.timestamp)}</p>
      </div>
      <div className="hidden flex-wrap justify-end gap-2 sm:flex"><Button variant="outline" onClick={() => query.refetch()}><RefreshCw className="h-4 w-4" />Refresh Symbols</Button>{actions.map((action) => <Button key={action.label} variant="outline" disabled={!action.allowed || query.action.isPending} onClick={() => command(action.label, action.path)}>{action.label}</Button>)}<Button variant="outline" onClick={exportReport}><Download className="h-4 w-4" />Export Report</Button></div>
      <div className="sm:hidden"><DropdownMenu><DropdownMenuTrigger asChild><Button variant="outline"><Menu className="h-4 w-4" />Actions</Button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem onSelect={() => query.refetch()}>Refresh Symbols</DropdownMenuItem>{actions.map((action) => <DropdownMenuItem key={action.label} disabled={!action.allowed} onSelect={() => command(action.label, action.path)}>{action.label}</DropdownMenuItem>)}<DropdownMenuItem onSelect={exportReport}>Export Report</DropdownMenuItem></DropdownMenuContent></DropdownMenu></div></div>
      {notice ? <p className="border-t border-slate-100 px-5 py-2.5 text-xs font-semibold text-blue-700">{notice}</p> : null}
    </section>
    <section className="grid gap-3 sm:grid-cols-2 md:grid-cols-3 xl:grid-cols-6">{data.kpis.map((kpi) => <Card key={kpi.label} className={cn("border-t-4", borders[kpi.status])}><CardContent className="p-3.5"><p className="text-[10px] font-semibold uppercase tracking-wide text-slate-500">{kpi.label}</p><p className="mt-2 truncate text-xl font-semibold">{kpi.value}</p><p className="mt-1 truncate text-[11px] text-slate-500">{kpi.detail}</p></CardContent></Card>)}</section>
    <Card><Heading icon={Workflow} title="Symbol Synchronization Workflow" detail="Instrument discovery through identity validation, feed qualification, risk readiness, and auditable control." /><CardContent className="overflow-x-auto"><div className="flex min-w-[1280px] gap-2">{data.workflow.map((node) => <div key={node.title} className="w-44 shrink-0 rounded-xl border border-slate-100 bg-slate-50/60 p-3"><Status value={node.status} /><p className="mt-3 text-xs font-semibold">{node.title}</p><p className="mt-2 text-[11px] text-slate-500">{node.symbolCount} pass / {node.failureCount} failed</p><p className="text-[11px] text-slate-500">{node.averageDelayMs} ms average</p>{node.aiRecommendation ? <p className="mt-2 text-[11px] font-medium text-purple-700">{node.aiRecommendation}</p> : null}</div>)}</div></CardContent></Card>
    <Card><Heading icon={ShieldCheck} title="Symbol Mapping Registry" detail="Searchable broker contract mapping, spread condition, tick freshness, and controlled corrections." /><CardContent><div className="mb-4 flex flex-col gap-2 md:flex-row md:justify-between"><label className="flex h-10 items-center gap-2 rounded-lg border border-slate-200 px-3 text-sm md:w-96"><Search className="h-4 w-4 text-slate-400" /><input aria-label="Search synchronized symbols" className="w-full outline-none" placeholder="Search symbol, broker, asset class..." value={search} onChange={(event) => setSearch(event.target.value)} /></label><select aria-label="Filter symbol status" className="rounded-lg border border-slate-200 px-3 text-xs" value={filter} onChange={(event) => setFilter(event.target.value)}><option>All</option><option>Healthy</option><option>Degraded</option><option>Critical</option><option>Offline</option></select></div><div className="overflow-x-auto"><table aria-label="Symbol synchronization registry" className="w-full min-w-[1820px] text-left text-xs"><thead className="border-y border-slate-100 bg-slate-50"><tr>{["Broker Symbol", "Normalized", "Broker / Server", "Asset Class", "Digits", "Contract", "Tick Value", "Spread / Baseline", "Trading", "Data Feed", "Mapping", "Last Tick", "Delay", "Gaps", "Risk", "Actions"].map((head) => <th key={head} className="px-3 py-3 uppercase text-slate-500">{head}</th>)}</tr></thead><tbody>{rows.map((symbol) => <tr key={symbol.id} className={cn("border-b border-slate-100", symbol.id === selected.id && "bg-blue-50/30")} onClick={() => setSelectedId(symbol.id)}><td className="px-3 py-3 font-semibold">{symbol.brokerSymbol}</td><td className="px-3 py-3">{symbol.normalizedSymbol}</td><td className="px-3 py-3">{symbol.brokerName}<p className="text-slate-500">{symbol.serverName}</p></td><td className="px-3 py-3">{symbol.assetClass}</td><td className="px-3 py-3">{symbol.digits}</td><td className="px-3 py-3">{symbol.contractSize.toLocaleString()}</td><td className="px-3 py-3">{symbol.tickValue}</td><td className="px-3 py-3">{symbol.spread} / {symbol.rollingSpread}</td><td className="px-3 py-3"><Badge variant={symbol.tradingAllowed ? "success" : "destructive"}>{symbol.tradingAllowed ? "Allowed" : "Blocked"}</Badge></td><td className="px-3 py-3"><Status value={symbol.feedStatus} /></td><td className="px-3 py-3"><Status value={symbol.mappingStatus} /></td><td className="px-3 py-3">{time(symbol.lastTickAt)}</td><td className="px-3 py-3">{symbol.tickDelaySeconds}s</td><td className="px-3 py-3">{symbol.gapCount}</td><td className="px-3 py-3"><Status value={symbol.riskLevel} /></td><td className="px-3 py-3" onClick={(event) => event.stopPropagation()}><DropdownMenu><DropdownMenuTrigger asChild><Button size="sm" variant="outline">Actions</Button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem onSelect={() => setSelectedId(symbol.id)}>View Instrument</DropdownMenuItem><DropdownMenuItem disabled={!data.permissions.canValidate} onSelect={() => command("Validate mapping", `/api/mt5/symbol-sync/symbols/${symbol.id}/validate`)}>Validate Mapping</DropdownMenuItem><DropdownMenuItem disabled={!data.permissions.canRemap} onSelect={() => command("Apply canonical mapping", `/api/mt5/symbol-sync/symbols/${symbol.id}/mapping`, { normalizedSymbol: symbol.brokerSymbol === "EURUSD.pro" ? "EURUSD" : symbol.normalizedSymbol }, "PATCH")}>Apply Canonical Mapping</DropdownMenuItem></DropdownMenuContent></DropdownMenu></td></tr>)}</tbody></table></div></CardContent></Card>
    <section className="grid gap-4 xl:grid-cols-[0.8fr_1.2fr]">
      <Card><Heading icon={CheckCircle2} title="Selected Instrument Detail" detail="Contract specification and current execution safety checks." /><CardContent className="space-y-4"><div className="rounded-xl bg-slate-50 p-4"><p className="text-lg font-semibold">{selected.brokerSymbol} <span className="text-slate-400">to</span> {selected.normalizedSymbol}</p><p className="text-xs text-slate-500">{selected.brokerName} / {selected.serverName} / {selected.assetClass}</p></div><div className="grid grid-cols-2 gap-3 text-xs">{[["Contract size", selected.contractSize.toLocaleString()], ["Tick size", String(selected.tickSize)], ["Tick value", String(selected.tickValue)], ["Lot limits", `${selected.minLot}-${selected.maxLot}`], ["Last sync", time(selected.lastSyncAt)], ["Trading", selected.tradingAllowed ? "Execution ready" : "Blocked"]].map(([label, value]) => <div key={label} className="rounded-lg border border-slate-100 p-3"><p className="text-slate-500">{label}</p><p className="mt-1 font-semibold">{value}</p></div>)}</div>{selected.mismatchReason ? <p className="rounded-lg border border-red-100 bg-red-50 p-3 text-xs text-red-700">{selected.mismatchReason}</p> : null}<Gauge label="Tick Delay (seconds)" value={selected.tickDelaySeconds} dangerous={selected.tickDelaySeconds > 15} /><Gauge label="Spread vs Baseline (%)" value={Math.round((selected.spread / selected.rollingSpread) * 25)} dangerous={selected.spread > selected.rollingSpread * 2} /></CardContent></Card>
      <Card><Heading icon={Signal} title="Market Data Feed & Spread Monitor" detail="Live tick delay, data gaps, and spread widening by broker instrument." /><CardContent className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-xs"><thead className="border-y border-slate-100 bg-slate-50"><tr>{["Instrument", "Broker", "Feed", "Tick Delay", "Spread", "Rolling Spread", "Gap Count", "Last Tick"].map((head) => <th key={head} className="px-3 py-3 uppercase text-slate-500">{head}</th>)}</tr></thead><tbody>{data.feedMetrics.map((metric) => <tr key={metric.id} className="border-b border-slate-100"><td className="px-3 py-3 font-semibold">{metric.normalizedSymbol}</td><td className="px-3 py-3">{metric.brokerName}</td><td className="px-3 py-3"><Status value={metric.feedStatus} /></td><td className="px-3 py-3">{metric.tickDelaySeconds}s</td><td className="px-3 py-3">{metric.spread}</td><td className="px-3 py-3">{metric.rollingSpread}</td><td className="px-3 py-3">{metric.gapCount}</td><td className="px-3 py-3">{time(metric.lastTickAt)}</td></tr>)}</tbody></table></CardContent></Card>
    </section>
    <section className="grid gap-4 xl:grid-cols-[1.05fr_0.95fr]">
      <Card><Heading icon={AlertTriangle} title="Mismatches & Feed Exceptions" detail="Instrument identity conflicts, missing ticks, delayed feeds, and spread anomalies." /><CardContent><div className="mb-3 flex flex-wrap gap-2">{["All", "Critical", "Warning", "Mapping Mismatch", "Missing Tick", "Delayed Tick", "Spread Anomaly"].map((option) => <button key={option} className={cn("rounded-full border px-3 py-1 text-xs", issueFilter === option ? "border-blue-200 bg-blue-50 text-blue-700" : "border-slate-200 text-slate-600")} onClick={() => setIssueFilter(option)}>{option}</button>)}</div><div className="overflow-x-auto"><table className="w-full min-w-[940px] text-left text-xs"><thead className="border-y border-slate-100 bg-slate-50"><tr>{["Instrument", "Broker", "Issue", "Severity", "Details", "Recommendation"].map((head) => <th key={head} className="px-3 py-3 uppercase text-slate-500">{head}</th>)}</tr></thead><tbody>{visibleIssues.map((issue) => <tr key={issue.id} className="border-b border-slate-100"><td className="px-3 py-3 font-semibold">{issue.brokerSymbol}<p className="text-slate-500">{issue.normalizedSymbol}</p></td><td className="px-3 py-3">{issue.brokerName}</td><td className="px-3 py-3">{issue.issueType}</td><td className="px-3 py-3"><Severity value={issue.severity} /></td><td className="px-3 py-3">{issue.detail}</td><td className="px-3 py-3 text-purple-700">{issue.recommendedAction}</td></tr>)}</tbody></table></div></CardContent></Card>
      <Card><Heading icon={Bot} title="AI Symbol Diagnostics" detail="Autonomous assessment of mapping errors, feed outages, and unsafe spread behavior." /><CardContent className="space-y-3">{data.diagnostics.map((diagnostic) => <div key={diagnostic.id} className="rounded-xl border border-purple-100 bg-purple-50/40 p-4"><div className="flex justify-between gap-2"><p className="font-semibold">{diagnostic.issue}</p><Severity value={diagnostic.severity} /></div><p className="mt-1 text-xs text-slate-500">{diagnostic.affectedInstrument}</p><p className="mt-3 text-xs"><strong>Root cause:</strong> {diagnostic.rootCause}</p><p className="mt-2 text-xs"><strong>Trading impact:</strong> {diagnostic.tradingImpact}</p><p className="mt-2 text-xs font-medium text-purple-800"><strong>Recommendation:</strong> {diagnostic.recommendation}</p><div className="mt-3 flex flex-wrap items-center gap-2"><Badge variant="purple">Confidence {Math.round(diagnostic.confidenceScore * 100)}%</Badge>{diagnostic.escalationRequired ? <Badge variant="destructive">Escalation required</Badge> : null}<Button size="sm" disabled={!data.permissions.canAutoRemediate || !diagnostic.autoFixEligible} onClick={() => command("Symbol safe recovery", "/api/mt5/symbol-sync/auto-remediate", { diagnosticId: diagnostic.id })}>Safe Recovery</Button></div></div>)}</CardContent></Card>
    </section>
  </div>;
}
